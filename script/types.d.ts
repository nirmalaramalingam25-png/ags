// Temporary shims for editor until project dependencies are installed
// Add proper types by installing packages and @types packages later
declare module "esbuild";
declare module "vite";
declare module "fs/promises";
